- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.gitconfig:
    ~/.vimrc:
- create:
    - ~/work

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
    - echo "hey you guys"
    - sleep 5
    - 
      command: ./test.zsh
      stdout: true
      stderr: true
