- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.gitconfig:
    ~/.vimrc:
- create:
    - ~/work

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdin: true
      stdout: true
      stderr: true
